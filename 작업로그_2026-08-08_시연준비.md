# 작업 로그 — 2026-08-11(월) 시연 준비 점검·수정 내역

작성일: 2026-08-08 (금)
목적: 침수(수체) 분석 파이프라인 전체 점검 및 월요일 시연 준비

---

## 1. 전체 점검 결과 요약

| 파트 | 상태 | 비고 |
|---|---|---|
| `pipeline_ui` (Streamlit 시연 데모) | ✅ 동작 | 서버 기동·전 기능·브라우저 클릭 스루 검증 완료 |
| `2_time_series_prediction` (ConvLSTM) | ⚠️ 부분 동작 | 문법 오류는 수정, 데이터·가중치·TF 환경 부재로 학습/추론 재현 불가 |
| `1_water_body_detection` (U-Net 노트북) | ❌ 실행 불가 | `WB_TSX.py`(모델 정의) 미커밋, 데이터(pkl)·가중치(h5)·원본 tif 전무 |

핵심 사실:
- 시연 데모는 **실모델 추론이 아니라 합성 시뮬레이션**입니다 (README에 명시된 설계). 저장소 전체에 모델 가중치 파일(.h5 등)은 0개.
- 실측 성능 근거는 Module2 노트북 출력 기록: **U-Net Acc 0.9777, 수체 IoU 0.8386, F1 0.9122** (재현은 현재 불가 — 발표에서 "검증 기록 기준"으로 인용).
- 시연 판정 흐름은 이제 결정론적으로 고정됨 (아래 3-2 참고).

---

## 2. 적용한 수정 사항

### 2-1. 버그 수정
| 파일 | 내용 |
|---|---|
| `2_time_series_prediction/train_conv_lstm.py:53` | 주석 `#` 누락으로 IndentationError → 수정, 컴파일 통과 |
| `pipeline_ui/app.py` (fetch_weather) | `hash(region)` → `zlib.crc32` — 실행할 때마다 기상 수치가 달라지던 비재현성 해결. 발표자료와 라이브 수치가 항상 일치 |

### 2-2. UI 결함 수정 (app.py)
- 사이드바 **'관측 일자' 흰 배경+흰 글자** → 어두운 배경(#123047)으로 수정, 값 가독 확인
- **빈 흰색 step-card 박스 8곳 제거** (Streamlit이 div 래퍼를 자동 닫아 빈 껍데기만 남던 문제) + `.lead` 스타일 독립 적용(15px)
- **위험 평가 ↔ 3세부 연계 태그 모순 통일** — 태그가 risk_label 기준을 따르고, 정상일 때 ⚠️ 대신 ✅ 표시. 임계값 표기도 "±5% 주의 · ±15% 위험"으로 정정
- '탐지 모델' 메트릭 카드 U-Net/U-Net 중복 표기 수정
- Streamlit **Deploy 버튼·Plotly 모드바 숨김** (시연 화면 정리)

### 2-3. 수체 형상 교체 — "미키마우스" 문제 해결
- 원인: `gen_sar_scene`이 원형 블롭만 생성 → 어떤 시드로도 동그라미 조합(미키 실루엣)만 나옴 (시드 36개 전수 확인)
- 해결: **사행(蛇行) 주 수로 + 지류 + 하구 확장** 형태의 `gen_water_mask` 신규 구현 (로컬 윈도우 스탬핑으로 12~14ms, 슬라이더 시연에 지장 없음)
- 시나리오 시드: 부산 7 / 한강 31 / 광주 89 (`seed_map`)

### 2-4. predict_future 재작성 — 시연 내러티브 고정
- 문제: 강 형상은 둘레/면적 비율이 커서 기존 픽셀 팽창 방식으로는 세 시나리오 모두 "홍수 위험"이 뜸
- 해결: **면적 비례 성장률** 방식으로 교체 (경계 픽셀을 `g = clip(0.006×(강수MA+바이어스−1.5), −0.08, +0.09)` 비율만큼만 추가/제거) + 시나리오 바이어스(홍수 +10 / 가뭄 −15)
- 검증된 판정 (기본 M=3, M=5에서도 유지):

| 시나리오 | 변화율 | 판정 |
|---|---|---|
| 부산 낙동강 하구 (기본) | +9.3% | 변화 주의 |
| 광주 - 홍수 | +28.0% | 홍수 위험 |
| 한강 - 가뭄 | −16.9% | 가뭄 위험 |

### 2-5. 발표 차트 결함 수정 (make_charts.py)
- 04번: x축 'T₀' 깨진 글자(tofu) → mathtext `$T_0$`
- 01번: 'Early Stop' 주석과 범례 겹침 → 위치 이동
- 06번: 'Validation (10%' 잘림 → 'Val 10%' 축약, 노란 배경 흰 글자 → 남색
- 07번: 파이프라인 다이어그램 노란 블록 글자 대비 동일 적용

---

## 3. 재생성 산출물 (모두 새 버전)

- `pipeline_ui/charts/` — 차트 7종 재생성
- `pipeline_ui/screens/` — 스크린샷 17장 전체 재캡처 (구버전 2007~2010 축 화면 폐기)
- `pipeline_ui/발표자료_파이프라인.pptx` — 13슬라이드 재생성 (4.2MB). 삽입 미디어 12개 전부 재생성본과 해시 일치 확인, 구버전 이미지 0개

참고: `06_complete_full.png` 등 "전체 페이지" 캡처 4장은 최신 Streamlit의 내부 스크롤 구조 때문에 뷰포트 캡처의 사본이 됨 — **PPT는 해당 파일을 쓰지 않아 시연에 영향 없음** (추후 캡처 스크립트 개선 여지).

---

## 4. 검증 내역

- app.py 컴파일 + 핵심 함수(gen_sar_scene → detect_water → fetch_weather → predict_future) 직접 호출 통과
- Streamlit 서버 기동 → HTTP 200 → **실제 브라우저 STEP1→4 클릭 스루** 통과 (DOM 검사: 빈 박스 0개, 툴바 숨김, 날짜 가독)
- **시연 리허설 통과**: 부산 기본 → 파이프라인 초기화 → 광주 홍수 → 초기화 → 한강 가뭄, 실제 브라우저 클릭으로 판정까지 확인
- 각 캡처·검증 후 서버 종료·포트 폐쇄 확인

---

## 5. 시연 가이드 (월요일)

**기동** (run.sh 금지 — 매번 pip install을 시도해 발표장 네트워크가 막히면 실패):
```bash
cd pipeline_ui && .venv/bin/streamlit run app.py --server.port 8501
```

**시연 순서**: 부산(기본) STEP1→4 → **파이프라인 초기화** → 광주 홍수 → "홍수 위험" → **초기화** → 한강 가뭄 → "가뭄 위험" → 3세부 연계 태그로 마무리

**수칙**:
1. 시나리오 전환 전 **반드시 "파이프라인 초기화" 클릭** (기상 데이터가 세션에 캐시되어 있음)
2. 기상청 **API 키 입력 금지** — 실제 호출 없이 "✓ API 연결"이 떠서 허위 시연이 됨
3. **GeoTIFF 업로드 금지** (PIL 로더 크래시 가능) — 즉석 업로드 요청엔 png/jpg 샘플로 대응
4. "실모델이 도는 거냐" 질문 → "파이프라인 흐름 시각화 데모이며, 정량 성능은 검증 기록 기준(U-Net 수체 IoU 0.8386)" 으로 답변
5. 백업 플랜: 서버 장애 시 `screens/` 17장 + PPT로 정지화면 시연 전환

---

## 6. 남은 작업

### 사용자 결정 대기
- [ ] `pipeline_ui/` **git 커밋** — 현재 untracked, 실수 한 번에 시연 자산 전체 유실 위험
- [ ] `2_time_series_prediction/examples/req_weather_data.py:12` **기상청 API 키 평문 커밋** — 재발급·환경변수화
- [ ] 시연 서버/빔프로젝터에서 리허설 1회

### 중장기 (시연 이후)
- [ ] `WB_TSX.py` / `S1_data.py` 복구 (U-Net 모델 정의 원본 미커밋 — 원저자 환경/도커에서 회수)
- [ ] 데이터(TSX_ai_data.pkl)·가중치(TSX_Unet_Commit.h5) 외부 백업 확인 및 확보
- [ ] 실모델 연동: `detect_water()`/`predict_future()`를 실제 추론 wrapper로 교체
- [ ] Module2 노트북 버그 2건 (fbeta0/IoU0 NameError, 추론 루프 마지막 9패치 누락)
- [ ] `build_model` 4개 파일 중복 정의 통합 (3벌은 Keras 3에서 빌드 실패 위험)
- [ ] requirements.txt에 matplotlib·python-pptx·playwright 추가
- [ ] GPU 드라이버 정합 (NVML driver/library version mismatch)
